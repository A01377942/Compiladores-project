
main() {
    var option, num;
    option=0;
    loop {
        prints("Input a number: ");
        num = readi();
        prints("Conversion to binary of that number: ");

        if (option>=1) {
           prints("Bingo ");
            
            break;
        }

        inc option;
    }
}
